{% extends 'base.html.twig' %}

{% block title %}Room Details{% endblock %}

{% block body %}
<div class="container">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Room {{ id }}</h5>
            <p class="card-text">Details about Room {{ id }}</p>
        </div>
    </div>
</div>
{% endblock %}
